# Database Configuration (MySQL - optional)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=qr_attendance

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# SQLite Database Path (used when MySQL is not available)
SQLITE_DB_PATH=./attendance.db

# CORS Configuration
FRONTEND_URL=http://localhost:3000